<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Demo"
        tools:targetApi="34">
        <activity
            android:name=".NewTaskSheet"
            android:exported="false" />
        <activity
            android:name=".Todolist"
            android:exported="false" />
        <activity
            android:name=".Baothuc"
            android:exported="false" />
        <activity android:name=".HomeFragment" />
        <activity
            android:name=".CLgiacngu"
            android:exported="false" />
        <activity
            android:name=".Timingsleep"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="android.app.gl.EGLConfigChooser"
            android:value="EGL_SWAP_BEHAVIOR_DISCARD" />
    </application>

</manifest>